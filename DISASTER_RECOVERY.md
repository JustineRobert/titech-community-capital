# Disaster Recovery

Required controls: database backup, restore verification, point-in-time recovery where supported, queue/outbox recovery, dead-letter replay, ledger/balance rebuild and reconciliation replay.

Current remediation package does not claim a completed restore exercise. That remains a production-readiness gate.
