apiVersion: v1
kind: Service
metadata:
  name: fintech-service
  labels:
    app: fintech
spec:
  type: LoadBalancer
  selector:
    app: fintech
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  sessionAffinity: None
