name: Deploy TITech App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # ✅ Checkout source
      - name: Checkout
        uses: actions/checkout@v3

      # ✅ Node.js setup for guardrail script
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # ✅ Install dependencies
      - name: Install dependencies
        run: npm install mongoose

      # 🚨 Guardrail: Check MongoDB indexes (Production strict, Staging auto-drop)
      - name: Check MongoDB indexes
        run: NODE_ENV=${{ secrets.ENVIRONMENT }} node scripts/checkIndexes.js
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}

      # ✅ Set up Docker Buildx for multi-platform builds
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # ✅ Cache Docker layers for faster builds
      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # ✅ Log in to DockerHub securely
      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      # ✅ Build and push Docker image
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: your-dockerhub/titech-app:latest

      # ✅ Set up kubectl
      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: "v1.29.0"

      # ✅ Configure kubeconfig from secret
      - name: Configure Kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > ~/.kube/config

      # ✅ Deploy to Kubernetes
      - name: Deploy to Kubernetes
        run: kubectl apply -f k8s/

      # ✅ Verify rollout status
      - name: Verify Deployment
        run: kubectl rollout status deployment/titech-app
