builder
    .register(requestId)
    .register(correlationId)
    .register(logger)
    .register(authentication)
    .register(authorization);

    1 Request ID

2 Correlation ID

3 Logger

4 Authentication

5 Authorization