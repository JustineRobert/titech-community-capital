# monitoring/prometheus.yml

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "titech-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:5000"] # Node.js backend server
